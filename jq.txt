get node creator from idb JSON
	.[].creator
get product creator from iDB JSON
	.[].products[].config.CREATOR
get customer from iDB JSON	
	.[].products[].config.CUSTOMER
identify SDC host of node
	.[].compute_node
Select example
	jq '.destinations[] | select(.destination == "acmelabs.space") '
example with awscli
	aws "--region=${region}" ec2 describe-subnets | jq -r .Subnets[].Ipv6CidrBlockAssociationSet[].Ipv6CidrBlock
